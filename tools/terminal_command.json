{
    "type": "function",
    "function": {
        "name": "terminal_command",
        "description": "Execute a linux zsh terminal command; Return input, output and exit code",
        "parameters": {
            "type": "object",
            "properties": {
                "command": {
                    "type": "string",
                    "description": "The whole terminal command to be executed."
                }
            },
            "required": ["command"]
        }
    }
}
